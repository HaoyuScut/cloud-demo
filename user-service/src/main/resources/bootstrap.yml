spring:
  application:
    name: userservice
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: nacos:8848 #nacos地址
#      server-addr: localhost:80
      config:
        file-extension: yaml
        namespace: public