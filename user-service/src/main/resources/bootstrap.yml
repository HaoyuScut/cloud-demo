spring:
  application:
    name: userservice
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
#      server-addr: localhost:80
      config:
        file-extension: yaml
        namespace: public